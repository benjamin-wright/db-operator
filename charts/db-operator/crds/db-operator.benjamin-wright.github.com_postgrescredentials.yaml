---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: postgrescredentials.db-operator.benjamin-wright.github.com
spec:
  group: db-operator.benjamin-wright.github.com
  names:
    categories:
    - games-hub
    kind: PostgresCredential
    listKind: PostgresCredentialList
    plural: postgrescredentials
    shortNames:
    - pgcred
    singular: postgrescredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.databaseRef
      name: Database
      type: string
    - jsonPath: .spec.username
      name: Username
      type: string
    - jsonPath: .spec.secretName
      name: Secret
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PostgresCredential is the Schema for the postgrescredentials API.
          It defines a PostgreSQL user whose credentials are stored in a Kubernetes Secret.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresCredentialSpec defines the desired state of PostgresCredential.
            properties:
              databaseRef:
                description: |-
                  DatabaseRef is the name of the PostgresDatabase resource in the same namespace
                  that this credential targets.
                minLength: 1
                type: string
              permissions:
                description: |-
                  Permissions is the list of per-database privilege entries for this credential.
                  Each entry specifies one or more databases and the privileges to grant in them.
                items:
                  description: |-
                    DatabasePermissionEntry maps a set of table-level privileges to one or more
                    logical databases within the target PostgreSQL instance.
                  properties:
                    databases:
                      description: |-
                        Databases is the list of PostgreSQL database names this entry applies to.
                        Each database will be created inside the target instance if it does not already exist.
                      items:
                        type: string
                      minItems: 1
                      type: array
                    permissions:
                      description: Permissions is the set of table-level privileges
                        to grant in those databases.
                      items:
                        description: DatabasePermission represents a PostgreSQL privilege
                          that can be granted to a user.
                        enum:
                        - SELECT
                        - INSERT
                        - UPDATE
                        - DELETE
                        - TRUNCATE
                        - REFERENCES
                        - TRIGGER
                        - ALL
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - databases
                  - permissions
                  type: object
                type: array
              secretName:
                description: |-
                  SecretName is the name of the Kubernetes Secret that will be created (or
                  updated) with the generated credentials. The Secret is written in the same
                  namespace as the PostgresCredential.
                minLength: 1
                type: string
              username:
                description: |-
                  Username is the PostgreSQL role/user to create inside the target database.
                  Must be 1–63 characters.
                maxLength: 63
                minLength: 1
                type: string
            required:
            - databaseRef
            - secretName
            - username
            type: object
          status:
            description: PostgresCredentialStatus defines the observed state of PostgresCredential.
            properties:
              conditions:
                description: Conditions contains detailed status conditions for the
                  PostgresCredential.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                default: Pending
                description: Phase is the current lifecycle phase of the credential.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
              secretName:
                description: SecretName is the name of the Kubernetes Secret that
                  was created for this credential.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
