---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: natsaccounts.db-operator.benjamin-wright.github.com
spec:
  group: db-operator.benjamin-wright.github.com
  names:
    categories:
    - games-hub
    kind: NatsAccount
    listKind: NatsAccountList
    plural: natsaccounts
    shortNames:
    - natsacct
    singular: natsaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NatsAccount is the Schema for the natsaccounts API.
          Each CR represents one account within the referenced NatsCluster. Multiple
          accounts in a single cluster are created by deploying multiple NatsAccount CRs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NatsAccountSpec defines the desired state of NatsAccount.
            properties:
              clusterRef:
                description: |-
                  ClusterRef is the name of the NatsCluster resource in the same namespace
                  that this account belongs to.
                minLength: 1
                type: string
              exports:
                description: Exports is the list of subjects this account exposes
                  to other accounts.
                items:
                  description: NatsExport defines a subject exported from this account
                    to other accounts.
                  properties:
                    subject:
                      description: Subject is the subject or wildcard pattern exported
                        from this account.
                      minLength: 1
                      type: string
                    tokenRequired:
                      description: |-
                        TokenRequired indicates that importing accounts must supply an activation token.
                        When true, this is a private export.
                      type: boolean
                    type:
                      description: Type is whether this export is a one-way stream
                        or a request/reply service.
                      enum:
                      - stream
                      - service
                      type: string
                  required:
                  - subject
                  - type
                  type: object
                type: array
              imports:
                description: Imports is the list of subjects this account brings in
                  from other accounts.
                items:
                  description: NatsImport defines a subject imported into this account
                    from another account.
                  properties:
                    account:
                      description: Account is the name of the NatsAccount CR (in the
                        same namespace) to import from.
                      minLength: 1
                      type: string
                    localSubject:
                      description: |-
                        LocalSubject remaps the imported subject in the local account namespace.
                        When omitted, the remote subject is used without modification.
                      type: string
                    subject:
                      description: Subject is the subject or wildcard pattern in the
                        source account to import.
                      minLength: 1
                      type: string
                    type:
                      description: Type is whether this import is a one-way stream
                        or a request/reply service.
                      enum:
                      - stream
                      - service
                      type: string
                  required:
                  - account
                  - subject
                  - type
                  type: object
                type: array
              users:
                description: |-
                  Users is the list of NATS users defined within this account. The operator
                  generates a password for each user and writes credentials to the named Secret.
                items:
                  description: NatsUser defines a user within a NATS account.
                  properties:
                    permissions:
                      description: |-
                        Permissions defines publish and subscribe permissions for this user.
                        When omitted, the user inherits account-level defaults.
                      properties:
                        publish:
                          description: Publish defines the subjects the user may publish
                            to.
                          properties:
                            allow:
                              description: Allow is the list of subjects permitted
                                for this scope.
                              items:
                                type: string
                              type: array
                            deny:
                              description: Deny is the list of subjects denied for
                                this scope. Deny takes precedence over Allow.
                              items:
                                type: string
                              type: array
                          type: object
                        subscribe:
                          description: Subscribe defines the subjects the user may
                            subscribe to.
                          properties:
                            allow:
                              description: Allow is the list of subjects permitted
                                for this scope.
                              items:
                                type: string
                              type: array
                            deny:
                              description: Deny is the list of subjects denied for
                                this scope. Deny takes precedence over Allow.
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    secretName:
                      description: |-
                        SecretName is the name of the Kubernetes Secret into which the operator
                        writes the generated credentials. The Secret is written in the same
                        namespace as the NatsAccount.
                      minLength: 1
                      type: string
                    username:
                      description: Username is the name of the NATS user.
                      minLength: 1
                      type: string
                  required:
                  - secretName
                  - username
                  type: object
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: NatsAccountStatus defines the observed state of NatsAccount.
            properties:
              conditions:
                description: Conditions contains detailed status conditions for the
                  NatsAccount.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                default: Pending
                description: Phase is the current lifecycle phase of the account.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
