image:
  repository: localhost:5001/db-operator
  tag: latest
  pullPolicy: IfNotPresent

instanceName: ""

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

mcp:
  enabled: true
  image:
    repository: localhost:5001/db-mcp
    tag: latest
    pullPolicy: IfNotPresent
  addr: ":8080"
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 64Mi
